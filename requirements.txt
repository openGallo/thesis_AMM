# thesis_AMM — public repo dependencies
# Data import and processing pipeline only
# (analysis scripts live in the private companion repo)

# Core data processing
numpy>=2.1
pandas>=2.2
pyarrow>=17.0        # parquet I/O for processed datasets

# HTTP / API
requests>=2.31

# Environment variables (THEGRAPH_API_KEY loading)
python-dotenv>=1.0

# Optionally used by process_data scripts
scipy>=1.14
statsmodels>=0.14
